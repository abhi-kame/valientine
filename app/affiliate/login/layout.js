export const metadata = {
  title: 'Partner Login',
  description: 'Log in to your ValenTiny partner dashboard to track clicks, conversions, and view payout status.',
};

export default function AffiliateLoginLayout({ children }) {
  return <>{children}</>;
}
