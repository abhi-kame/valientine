export const metadata = {
  title: 'Valentine Secret Proposal ❤️',
  description: 'A special surprise for someone special.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function ProposalLayout({ children }) {
  return <>{children}</>;
}
